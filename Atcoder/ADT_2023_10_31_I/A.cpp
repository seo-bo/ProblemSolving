#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void)
{
	cin.tie(0)->sync_with_stdio(0);
	long double A, B;
	cin >> A >> B;
	cout << fixed << setprecision(3) << B / A;
	return 0;
}
