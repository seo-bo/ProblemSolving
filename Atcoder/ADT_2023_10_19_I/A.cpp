#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void)
{
	cin.tie(0)->sync_with_stdio(0);
	long double a;
	cin >> a;
	cout << fixed << setprecision(15) << a / 100;
	return 0;
}
