#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(void)
{
	cin.tie(0)->sync_with_stdio(0);
	double a = 0, b = 0;
	cin >> a >> b;
	cout << fixed << setprecision(13) << a / b;
	return 0;
}