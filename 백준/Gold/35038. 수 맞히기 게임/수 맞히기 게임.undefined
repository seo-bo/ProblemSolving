#include<bits/stdc++.h>
#include "game.h"
using namespace std;

int mj1000j(int N, int K)
{
	return N / (2 * max(1, K)) % 2;
}

int anchor(int M, int K, int C)
{
	return (!K) ? M : (M < K) ? M + K : ((M - K) / (2 * K) % 2 == C) ? M - K : M + K;
}